[tool.poetry]
name = "acme-status-dashboard"
version = "0.1.0"
description = "Internal status dashboard service"
authors = ["Matan Elbaz"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
flask = "3.1.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
